const Model = require('./PurchaseOrderModel.js');
function PurchaseOrderClass() {
  this.CreateNewInvoice = function (purchaseInfo) {
    // let invoice_qty = ticketsArray.length
    // let purchaser = eventInfo.purchaser
    return purchaseInfo
  }
}
